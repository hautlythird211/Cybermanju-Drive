[package]
name = "cybermanju-types"
version = "0.1.0"
edition = "2021"
description = "Shared data types for Cybermanju Drive"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
